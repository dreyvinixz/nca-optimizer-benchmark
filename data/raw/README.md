# Raw Data

This folder stores benchmark input datasets that should remain available even
when `experiments/` is not committed.

Current dataset:

```text
data/raw/merged_output.csv
```

This file was copied from the legacy IJCNN experiment folder and is now the
official input path for the clean NCA benchmark pipeline.
